[project]
name = "DDQN-project"
version = "0.1.0"
description = "DDQN project scaffold"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
